// Access the last element of an array using the length property.

let fruits = ["banana", "apple", "mango", "orange", "strawberry"];

let lastElement = fruits[fruits.length - 1];

console.log(lastElement);
